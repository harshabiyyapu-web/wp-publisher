services:
  wp-publisher:
    build: .
    restart: unless-stopped
    ports:
      - "3002:3000"
    environment:
      - NODE_ENV=production
      - DB_PATH=/data/dev.db
      - DATABASE_URL=file:/data/dev.db
    volumes:
      - wp_db_data:/data

volumes:
  wp_db_data:
